# Core scientific stack
numpy>=1.24
scipy>=1.10
pandas>=2.0
joblib>=1.3
requests>=2.28

# Parcellation atlases
nilearn>=0.10

# Phase 1: linear mixed-effects models.
# pymer4 wraps lme4 through rpy2 and therefore needs a working R installation
# with the lme4 and lmerTest packages; see the README for setup.
pymer4>=0.8
rpy2>=3.5,<3.5.13
statsmodels>=0.14

# Phase 2: Bayesian normative model.
# The ELPD comparison works with either arviz 0.x or 1.x.
bambi>=0.13
arviz>=0.15
pymc>=5.10

# Phase 2: classification
scikit-learn>=1.3

# Figures and progress reporting
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.65
